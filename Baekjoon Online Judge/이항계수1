/*
이항계수1
11050번
*/

#include<stdio.h>

int main() {
	int n, k;
	scanf("%d %d", &n, &k);
	int result1 = 1;
	int result2 = ;

	for (int i = 0; i < k; i++) {
		result1 *= n - i;
		result2 *= i+1;
	}
	printf("%d\n", result1 / result2);

	return 0;
}
